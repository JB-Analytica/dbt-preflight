name: seedshop
version: "1.0.0"
config-version: 2
profile: seedshop
model-paths: ["models"]
seed-paths: ["seeds"]
flags:
  send_anonymous_usage_stats: false
models:
  seedshop:
    +materialized: table
